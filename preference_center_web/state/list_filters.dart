enum TypeFilter {
  all,
  audience,
  journal,
  interest,
  news,
  service,
  other,
  unknown,
}

enum ErrorFilter {
  all,
  missingValue,
  textFormat,
  missingData,
  noCode,
  noErrors,
  onlyErrors,
}

enum RelationFilter { all, recommendationOf, recommendedBy, both }
